# Task Tracker

A simple command-line interface (CLI) task tracker application written in Python.

## Features

- Add new tasks
- List all tasks
- Delete tasks by ID
- Data persistence using JSON

## Usage

1. Run the application:
   ```bash
   python main.py
   ```

2. Follow the on-screen prompts to manage your tasks.

## Requirements

- Python 3.x
